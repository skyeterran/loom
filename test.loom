(let cat #sock)

(let trait (match cat
    (#sock #cute)
    (#salad #smart)))

(match trait
    (#cute (let msg "That's a cute kitty!"))
    (#smart (let msg "Woah, that's a lawyer.")))

(let user (object
    (species #dog)
    (name "Skye")
    (age 26)
    (stats (object (dex 4)
        (int 14)
        (str 8)))))

(let cat (object
    (species #cat)
    (name "Salad")
    (age 6)
    (stats (object
        (dex 18)
        (int 9)
        (str 2)))))
